// Server-side API calls are handled via Astro API routes in src/pages/api/.
// The SDK is used server-side only — never instantiated in the browser.
// Frontend components call /api/search, /api/card, /api/batch, /api/games via fetch().
export {};
